---
- name: Import Falco GPG key
  ansible.builtin.rpm_key:
    state: present
    key: https://falco.org/repo/falcosecurity-packages.asc

- name: Add Falco repository
  ansible.builtin.yum_repository:
    name: falcosecurity
    description: Falco Repository
    baseurl: https://download.falco.org/packages/rpm
    gpgcheck: yes

- name: Install Falco engine, Falcosidekick and eBPF dependencies
  ansible.builtin.dnf:
    name:
      - falco
      - falcosidekick
      - bpftool
    state: present

- name: Configure Falco for modern eBPF driver
  ansible.builtin.replace:
    path: /etc/falco/falco.yaml
    regexp: 'engine:\n  kind: module'
    replace: 'engine:\n  kind: modern_ebpf'
  notify: restart falco

- name: Deploy Falco Custom Threat Detection Rules
  ansible.builtin.template:
    src: falco_rules.local.yaml.j2
    dest: /etc/falco/falco_rules.local.yaml
    owner: root
    group: root
    mode: '0640'
  notify: restart falco

# FIX: Alertmanager-Weiterleitung via Falcosidekick einrichten
- name: Configure Falcosidekick Alertmanager Integration
  ansible.builtin.copy:
    dest: /etc/falcosidekick/falcosidekick.yaml
    content: |
      alertmanager:
        hostport: "http://{{ soc_hub_wg_ip }}:9093"
        minimumpriority: "warning"
    owner: root
    group: root
    mode: '0640'
  notify: restart falcosidekick

- name: Ensure Systemd Service Override directory exists
  ansible.builtin.file:
    path: /etc/systemd/system/falco.service.d
    state: directory
    owner: root
    group: root
    mode: '0755'

- name: Enterprise Systemd Hardening Service Override for Falco
  ansible.builtin.copy:
    dest: /etc/systemd/system/falco.service.d/override.conf
    content: |
      [Service]
      NoNewPrivileges=true
      ProtectSystem=full
      ProtectHome=read-only
      PrivateTmp=true
      ProtectClock=yes
      ProtectHostname=yes
      RestrictRealtime=yes
    owner: root
    group: root
    mode: '0644'
  notify: restart falco

- name: Enable and start Falco service
  ansible.builtin.systemd:
    name: falco
    enabled: true
    state: started
    daemon_reload: true

- name: Enable and start Falcosidekick service
  ansible.builtin.systemd:
    name: falcosidekick
    enabled: true
    state: started
    daemon_reload: true
